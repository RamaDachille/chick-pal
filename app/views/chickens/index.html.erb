<h1>Chickens</h1>
<%= link_to "Rent your chicken", new_chicken_path %>
<div id="chickens">
  <div class="cards">
    <% @chickens.each do |chicken| %>
      <%= render "chickens/chicken", chicken: chicken %>
      <p>
        <%= link_to "Show this chicken", chicken_path(chicken.id)%>
      </p>
    <% end %>
  </div>
</div>
