<div class="container-full main">
  <div id="chickens" class="container-full justify-content-between">
    <div class="chicken-row row cards">
      <% @chickens.each do |chicken| %>
        <%= render "chickens/chickenv2", chicken: chicken %>
      <% end %>
    </div>
    <div class="map">
      <h1> map </h1>
    </div>
  </div>

</div>
